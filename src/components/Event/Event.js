import React from 'react';
import { Button } from 'react-bootstrap';
import { Link } from 'react-router-dom';

const Event = () => {
    return (
        <div  className="container mx-auto row py-5 text-center">
            <h1>All The Tour Events</h1>
            
        </div>
    );
};

export default Event;